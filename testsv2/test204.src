func main
    var int a
    assign a 5
    if > a 0
        var bool a
        assign a True
        if a
            var int a
            assign a 7
            funccall print a
        endif
        funccall print a
    else 
        var int a
        assign a 6
    endif
    funccall print a
endfunc